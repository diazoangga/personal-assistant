# Direct localhost dev (no tunnel):
VITE_API_URL=http://localhost:8000
# Accessing the frontend through a tunnel (e.g. ngrok)? Use the Vite proxy
# instead of a separate backend origin, by setting this to an empty string:
# VITE_API_URL=
